<h1>Add a dose</h1>
<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.all, prompt: 'Select ingredient', input_html: { as: :select} %>
  <%= f.submit "Add dose", class: "btn btn-primary" %>
<% end %>
